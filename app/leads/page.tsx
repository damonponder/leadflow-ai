import LeadTable from "@/components/LeadTable";

export default function LeadsPage() {
    return(
        <main style={{ padding: "24px" }}>
        <h1>Leads</h1>
        <p>This is the leads page for LeadFlow AI</p>
        <LeadTable />
        </main>
    );
}